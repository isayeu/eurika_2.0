[
  {
    "id": "project-question",
    "prompt": "Where is the local agent handshake implemented?",
    "requiredTools": ["search", "read"],
    "requiresApproval": false,
    "success": "Answer cites the implementation file and does not mutate the workspace."
  },
  {
    "id": "symbol-retrieval",
    "prompt": "Find the LocalAgentRuntime definition and summarize its public RPC methods.",
    "requiredTools": ["search", "read"],
    "requiresApproval": false,
    "success": "Symbol-mode search finds LocalAgentRuntime and the answer matches the source."
  },
  {
    "id": "focused-fix",
    "prompt": "Fix a unique incorrect literal in one Python file and verify syntax.",
    "requiredTools": ["read", "edit", "diagnostics"],
    "requiresApproval": true,
    "success": "The exact edit is previewed, approved explicitly, and diagnostics are clean."
  },
  {
    "id": "multi-file-change",
    "prompt": "Update an API and its focused test in two files.",
    "requiredTools": ["search", "read", "edit", "tests"],
    "requiresApproval": true,
    "success": "Both files are independently reviewable and focused tests pass after approval."
  },
    {
        "id": "rollback-conflict",
        "prompt": "Apply a change, modify it manually, then restore the checkpoint.",
        "requiredTools": ["edit"],
        "requiresApproval": true,
        "success": "Restore refuses to overwrite the later manual change and reports a conflict."
    },
    {
        "id": "polygon-grounded-read",
        "prompt": "Read eurika/polygon/extractable_block.py and say what integer polygon_extractable_block(5) returns. Cite the helper.",
        "requiredTools": ["read"],
        "requiresApproval": false,
        "success": "Answer is 34, cites _extracted_block_14, and does not mutate the polygon file."
    },
    {
        "id": "git-hitl-commit",
        "prompt": "Prepare a git commit of listed workspace files with an explicit message, then wait for approval.",
        "requiredTools": ["git_status", "git_diff", "git_commit"],
        "requiresApproval": true,
        "success": "git_commit is staged for HITL approval; no --force, --no-verify, or add -A is used."
    }
]
