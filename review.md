Отлично. Сейчас будет не «пожелания», а **инженерная дорожная карта развития Eurika после 3.0.7**.

Я построю её как:

* 🎯 цель версии
* 🔧 конкретные действия
* 📦 ожидаемый результат
* ⚠ риски
* ✅ критерии готовности

Будем двигаться по фазам:
**Стабилизация → Архитектурное укрепление → Платформизация → Интеллектуализация → Продуктовый уровень**

> Update (R3 runtime hardening): no-op сценарии теперь явно показывают причины через `campaign_skipped`/`session_skipped`, telemetry синхронизирован (`skipped_count`, `no_op_rate`), а для безопасного точечного ретрая добавлен CLI-флаг `--allow-campaign-retry` (без глобального bypass campaign memory).
> Update (R4 planner stability): в планировщике добавлен предикат, который блокирует `split_module` для уже extracted-файлов (`*_extracted.py`), чтобы исключить шумовые артефакты вида `*_extracted_extracted.py` и снизить риск повторных no-op/регрессионных операций.

---

# 🚀 v3.1 — Архитектурная стабилизация

## 🎯 Цель

Остановить расползание архитектуры и зафиксировать слои.

---

## Шаг 1. Формально зафиксировать Layer Model

### 🔧 Действия

1. Создать `ARCHITECTURE.md`
2. Ввести строгую схему:

```
Layer 0 — Infrastructure
Layer 1 — Core Model
Layer 2 — Analysis
Layer 3 — Planning
Layer 4 — Execution
Layer 5 — Reporting
Layer 6 — CLI
```

3. Записать правило:

   * Импорт разрешён только вниз
   * Вверх запрещён

### 📦 Результат

Появляется архитектурный контракт.

### ✅ Done когда:

* Есть документ
* Все модули размечены по слоям

---

## Шаг 2. Разделить Domain и Presentation

### 🔧 Действия

1. Убрать Markdown из:

   * analysis
   * planning
   * execution

2. Ввести строгие dataclass-результаты.

### 📦 Результат

Domain возвращает структуры данных.
Reporting занимается форматированием.

### ✅ Done когда:

Ни один domain-модуль не возвращает строки отчёта.

---

## Шаг 3. Ограничить размер модулей

### 🔧 Действия

1. Правило:

   * > 400 строк — предупреждение
   * > 600 — обязательное деление
   
   Пример: architecture_planner.py (условно 550 строк)
❌ Было
architecture_planner.py
    - build_patch_plan
    - simulate
    - risk scoring
    - action mapping
    - markdown formatting
✅ Стало
planning/
    planner.py
    simulation.py
    risk_model.py
    action_mapper.py

2. Разделить:

   * architecture_planner
   * patch_apply
   * orchestration

### 📦 Результат

Нет файлов >600 строк.

---

## Шаг 4. Единый публичный API подсистем

Каждая подсистема должна иметь 1 входную точку:

```
analysis.run(snapshot)
planning.build(analysis_result)
execution.apply(plan)
reporting.render(result)
```

Всё остальное — private.
    Окей. Без воды — превращаем **v3.1** в конкретный, инженерный чек-лист.
Буду строгой и системной, как ты любишь 🙂

---

# 📌 Eurika — v3.1 Hardening & Stabilization Checklist

Цель версии 3.1:

> Сделать систему **предсказуемой, тестируемой и архитектурно устойчивой** перед масштабированием.

---

# 🔷 БЛОК 1 — Архитектурная чистка (обязательно)

## 1.1. Убрать размытые зоны ответственности

### ✅ Задачи:

* [ ] Убедиться, что `Agent` НЕ:

  * пишет в память напрямую
  * форматирует CLI-вывод
  * логирует напрямую
* [ ] Вся запись памяти → только через `memory_store()`
* [ ] Вынести формирование prompt в отдельный класс:

  * `PromptBuilder`
* [ ] Вынести reasoning в:

  * `ReasoningEngine`

---

## 1.2. Ввести чёткие слои

Структура должна стать такой:

```
core/
  agent.py
  reasoning_engine.py
  prompt_builder.py

memory/
  memory_store.py
  memory_reader.py

analysis/
  smells.py
  summary.py

cli/
  commands.py
```

### ✅ Задачи:

* [ ] Разнести файлы по папкам
* [ ] Убрать циклические импорты
* [ ] Проверить что нет import из cli внутрь core

---

# 🔷 БЛОК 2 — Контроль состояния

Сейчас Eurika может "поплыть", если reasoning падает.

## 2.1. Ввести явную модель состояния

Создать:

```python
class AgentState(Enum):
    IDLE
    THINKING
    ERROR
    DONE
```

### ✅ Задачи:

* [ ] Добавить state в Agent
* [ ] При запуске reasoning → state=THINKING
* [ ] При исключении → state=ERROR
* [ ] При завершении → state=DONE
* [ ] Добавить unit-тест на переходы состояний

---

# 🔷 БЛОК 3 — Тестирование (обязательно)

Если сейчас нет нормальных тестов — это слабое место.

## 3.1. Покрыть критические модули

### Минимальный набор:

* [ ] test_reasoning_engine.py
* [ ] test_memory_store.py
* [ ] test_prompt_builder.py
* [ ] test_agent_state.py

---

## 3.2. Проверить edge-cases

* [ ] пустой ввод
* [ ] огромный ввод
* [ ] ошибка модели
* [ ] ошибка записи в память
* [ ] нет сети

---

# 🔷 БЛОК 4 — Контроль логирования

Сейчас логирование может быть хаотичным.

## 4.1. Ввести единый логгер

Создать:

```python
get_logger(name: str)
```

### ✅ Задачи:

* [ ] Убрать print()
* [ ] Заменить на logging
* [ ] Добавить уровни:

  * DEBUG
  * INFO
  * WARNING
  * ERROR
* [ ] Добавить флаг verbose в CLI

---

# 🔷 БЛОК 5 — Контракты и типизация

Если ты хочешь серьёзный проект — без typing нельзя.

### ✅ Задачи:

* [ ] Проставить type hints в core
* [ ] Включить mypy
* [ ] Исправить ошибки типов
* [ ] Добавить dataclass там где DTO

---

# 🔷 БЛОК 6 — CLI стабилизация

Сейчас CLI — слабая точка.

### ✅ Задачи:

* [ ] Добавить help для каждой команды
* [ ] Добавить --debug флаг
* [ ] Сделать exit codes:

  * 0 — success
  * 1 — runtime error
  * 2 — invalid input

---

# 🔷 БЛОК 7 — Документация

## 7.1 Обновить README

* [ ] Архитектурная схема
* [ ] Как работает reasoning
* [ ] Как расширять систему
* [ ] Roadmap

---

# 🔷 БЛОК 8 — Мини-рефакторинг перед релизом

### Перед tag v3.1:

* [ ] убрать мёртвый код
* [ ] убрать TODO в продакшене
* [ ] убрать закомментированный код
* [ ] проверить линтером
* [ ] прогнать тесты
* [ ] проверить запуск на чистой системе

---

# 📊 Definition of Done для v3.1

Версия считается завершённой, если:

✔️ 80% core покрыто тестами
✔️ Нет циклических импортов
✔️ Agent не нарушает SRP
✔️ Есть state-machine
✔️ Логи централизованы
✔️ Документация обновлена

---

# 💥 Самые слабые места сейчас (честно)

1. Размытая ответственность Agent
2. Нестабильный reasoning fallback
3. Недостаточное тестирование
4. Отсутствие формальной state-модели
5. CLI недостаточно строгий

---

# 🎯 Что даст v3.1

* Проект станет **предсказуемым**
* Упростится масштабирование
* Появится основа для плагинов
* Будет легче внедрять online-анализ
* Ты перестанешь путаться в структуре

---

# 🚀 v3.2 — Архитектурная саморефлексия (Meta-Architecture)

## 🎯 Цель

Eurika должна проверять саму себя.

---

## Шаг 1. Layer Violation Detector

### 🔧 Реализовать:

* ✅ проверка импортов вверх
* ✅ детектор нарушения слоёв (L0–L6)

### 📦 Новый модуль:

```
eurika/checks/
    dependency_firewall.py
```

### 📌 Фактический статус:

* strict режим: `EURIKA_STRICT_LAYER_FIREWALL=1`
* контракт в `tests/test_dependency_guard.py`
* baseline: **0 violations / 0 waivers**

---

## Шаг 2. Complexity Budget

Ввести лимиты:

* max fan-in
* max fan-out
* max degree
* max file size

Если превышено → warning.

---

## Шаг 3. Централизационный индекс

Добавить метрику:

```
Centralization Index = max_degree / total_modules
```

Трекать динамику.

---

# 🚀 v3.3 — Декомпозиция и Подсистемизация

## 🎯 Цель

Превратить Eurika в модульную платформу.

---

## Шаг 1. Выделить Subsystems как пакеты

```
eurika/
    core/
    analysis/
    planning/
    execution/
    evolution/
    reporting/
```

Каждый subsystem:

* не знает о CLI
* экспортирует только 1 public API

---

## Шаг 2. Dependency Firewall

Ввести правило:
Subsystem не может импортировать sibling subsystem напрямую.
Только через public API.

---

## Шаг 3. Dependency Graph Contract Tests

Автотест, который:

* строит граф
* проверяет нарушения слоёв
* падает если есть вверх-импорт

---

# 🚀 v4.0 — Интеллектуальная архитектура

## 🎯 Цель

Eurika не просто анализирует — она предсказывает.

---

## Шаг 1. Architectural Risk Prediction

На основе истории:

* рост fan-in
* рост smells
* рост degree

Прогноз:

```
This module will become bottleneck in 3 iterations
```

---

## Шаг 2. Refactoring Recommendation Engine 2.0

Не просто:

> "разделить модуль"

А:

* какие классы вынести
* какие функции сгруппировать
* какие зависимости разорвать

---

## Шаг 3. Architectural Scoring Model

Ввести формальную модель:

```
Health Score =
    100
    - smell_penalty
    - centralization_penalty
    - size_penalty
    - violation_penalty
```

---

# 🚀 v4.1 — Плагинная архитектура

## 🎯 Цель

Сделать Eurika расширяемой.

---

## Шаг 1. Plugin Interface

```
class ArchitecturePlugin:
    def analyze(self, snapshot) -> Optional[Issue]
```

---

## Шаг 2. Dynamic Plugin Loader

* поиск модулей
* регистрация
* sandbox execution

---

## Шаг 3. External Analyzer Support

Подключение:

* mypy
* ruff
* pylint
* custom linters

---

# 🚀 v5.0 — Продуктовый уровень

## 🎯 Цель

Из исследовательского инструмента → в инженерный продукт.

---

## 1. REST API

```
POST /analyze
POST /plan
POST /apply
GET  /history
```

---

## 2. Web UI

* визуализация графа
* heatmap централизации
* динамика эволюции
* слой нарушений

---

## 3. CI Mode

GitHub Action:

* анализ PR
* блокировка merge при деградации

---

# 🔥 Самый важный момент

Тебе нельзя одновременно:

* добавлять фичи
* и укреплять архитектуру

Следующие 1–2 версии должны быть:

> только структурные

Если продолжишь расширять без стабилизации —
появится архитектурный долг.

---

# 📊 Стратегическая траектория

| Версия | Фокус              |
| ------ | ------------------ |
| 3.1    | стабилизация       |
| 3.2    | self-guard         |
| 3.3    | модульная чистота  |
| 4.0    | интеллектуальность |
| 4.1    | расширяемость      |
| 5.0    | продукт            |

---

# 🎯 Лично моё инженерное мнение

Если ты пройдёшь:

* v3.1 правильно
* v3.2 дисциплинированно

то Eurika станет архитектурным движком уровня:

> internal architecture intelligence system

Это уже серьёзный класс инструмента.

---

Если хочешь, дальше можем:

* разбить v3.1 на конкретный чек-лист задач
* сделать приоритетную матрицу
* или оценить трудозатраты по неделям

Как будем двигаться — стратегически или по спринтам?
